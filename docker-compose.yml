version: '3'
services:
  app:
    container_name: issue-tracker
    restart: always
    build: .
    ports:
      - "80:3000"
    links:
      - db
    depends_on:
      - db
    environment:
      - PORT=3000
      - HOST=db
      - PASSWORD=Wux70bah!
      - USER=root
      - DATABASE=issue_tracker
  db:
    container_name: db
    image: mysql
    restart: always
    ports:
      - 3306:3306
    environment:
      - MYSQL_ROOT_PASSWORD=Wux70bah!
